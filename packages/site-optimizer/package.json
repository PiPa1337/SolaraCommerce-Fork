{
  "name": "@solara/site-optimizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude src/oversize-snapshot.test.ts",
    "test:stress": "vitest run --no-file-parallelism src/oversize-snapshot.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@solara/project-schema": "workspace:*"
  }
}
