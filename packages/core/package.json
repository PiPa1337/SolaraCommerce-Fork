{
  "name": "@solara/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./performance": "./src/performance.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --no-file-parallelism",
    "test:ci": "vitest run --no-file-parallelism --pool=threads --poolOptions.threads.singleThread",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@solara/modules": "workspace:*",
    "@solara/project-schema": "workspace:*"
  }
}
